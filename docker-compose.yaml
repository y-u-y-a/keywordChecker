version: "3"
services:
  # mysql:
  #   container_name: python_mysql
  #   build:
  #     context: ./build/mysql
  #   ports:
  #     - 4306:3306
  #   volumes:
  #     - mysql-data:/var/lib/mysql
  #   restart: always

  server:
    container_name: keywordChecker_server
    build:
      context: ./build/server
    volumes:
      - ./code/server:/code
    ports:
      - 8000:8000
    working_dir: /code # コンテナ起動時のディレクトリを指定
    tty: true # コンテナを立ち上げっぱなしにする（ttyとは、標準出力の接続先デバイス）
    # depends_on:
    #   - mysql

  client:
    container_name: keywordChecker_client
    build:
      context: ./code/client
    ports:
      - 3000:80


networks:
  default:
    external: false

volumes:
  node-packages:
    external: false

# volumes:
#   mysql-data:
#     external: false
